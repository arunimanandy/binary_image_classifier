seed: 42
image_size: 224
data:
  raw_dir: data/raw
  cat_dir: data/raw/PetImages/Cat
  dog_dir: data/raw/PetImages/Dog
  processed_dir: data/processed
  train_ratio: 0.8
  val_ratio: 0.1
  test_ratio: 0.1
training:
  batch_size: 16
  epochs: 3
  learning_rate: 0.001
  num_workers: 0
model:
  artifact_path: models/catdog_cnn.pt
mlflow:
  experiment_name: cats-vs-dogs-baseline
  tracking_uri: file:./mlruns
